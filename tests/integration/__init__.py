"""
Integration tests for Avatar Engine.

These tests require real CLI tools (claude, gemini) and make actual API calls.
Run separately from unit tests:

    pytest tests/integration/ -v

Or skip them in normal runs:

    pytest tests/ --ignore=tests/integration/
"""
